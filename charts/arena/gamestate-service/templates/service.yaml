apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamestate-service.fullname" . }}
  labels:
    {{- include "gamestate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gamestate-service.selectorLabels" . | nindent 4 }}
