apiVersion: v1
kind: Service
metadata:
  name: {{ include "codex-on-air.fullname" . }}
  labels:
    {{- include "codex-on-air.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codex-on-air.selectorLabels" . | nindent 4 }}
