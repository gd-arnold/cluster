apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gamestate-service.fullname" . }}-test-connection"
  labels:
    {{- include "gamestate-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gamestate-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
